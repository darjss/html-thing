# Context Map

This repo uses a multi-context domain-doc layout. Each context has its own `CONTEXT.md` and `docs/adr/`.

## Contexts

| Context | Path | Owns |
| ------- | ---- | ---- |

<!-- Add a row per context as they emerge (e.g. cli/, r2-bucket/). The /domain-modeling skill populates this lazily. -->
